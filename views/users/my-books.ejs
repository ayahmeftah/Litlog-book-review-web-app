<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel="stylesheet" href="/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <title>My Books</title>
</head>
<body>
<%- include("../partials/navbar") %>

<div class="container mt-5">
  <h3>My Books (<%= books.length %>)</h3>

  <% if (books.length === 0) { %>
    <p class="text-muted">You haven't added any books yet</p>
  <% } else { %>
    <% books.forEach((book, i) => { %>
      <% if (i % 3 === 0) { %><div class="row mb-4"><% } %>

      <div class="col-md-4">
        <div class="card d-flex flex-row align-items-center p-2">
          <img src="<%= book.BookImage %>" alt="Cover" class="book-img-card">
          <div>
            <h5 class="mb-1"><%= book.title %></h5>
            <p class="mb-1"><strong>Genre:</strong> <%= book.genre %></p>
            <p class="mb-1"><strong>Rating:</strong> <%= book.averageRating ? (book.averageRating.toFixed(1) + ' / 5') : "N/A" %></p>
            <p class="mb-2"><strong>Year:</strong> <%= book.yearOfPublication || 'Unknown' %></p>
            <a href="/books/<%= book._id %>" class="btn btn-sm btn-primary">View Book</a>
          </div>
        </div>
      </div>

      <% if (i % 3 === 2 || i === books.length - 1) { %></div><% } %>
    <% }) %>
  <% } %>
</div>
</body>
</html>