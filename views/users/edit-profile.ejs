<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css" />
    <script src="/js/showPassword.js"></script>
    <title>Edit Profile</title>
</head>
<body>
<%- include("../partials/navbar") %>

<div class="container mt-5">
  <div class="signup-container">
    <h2 class="text-center mb-4">Edit Profile</h2>

    <form action="/users/profile/edit?_method=put" method="post" enctype="multipart/form-data">
      <div class="row align-items-start">
        <div class="col-md-4 text-center mb-4">
          <label class="form-label">Current Profile Picture</label><br>
          <img src="<%= user.profilePic || '/images/default-profile-img.jpg' %>" width="120" class="rounded mb-2 d-block mx-auto"><br>
          <input type="file" name="profilePic" class="form-control mb-2">
          <div class="form-check d-flex align-items-center justify-content-start gap-2 mt-2">
            <input type="checkbox" class="form-check-input" name="removePic" value="true" id="removePic">
            <label class="form-check-label mb-0" for="removePic">Remove current picture</label>
          </div>
        </div>

        <div class="col-md-8">
          <div class="row">
            <div class="col-md-6 mb-3">
              <label for="name" class="form-label">Name</label>
              <input type="text" name="name" id="name" class="form-control" value="<%= user.name %>">
              <% if (errorMessages?.name) { %>
                <div class="text-danger small"><%= errorMessages.name %></div>
              <% } %>
            </div>
            <div class="col-md-6 mb-3">
              <label for="username" class="form-label">Username</label>
              <input type="text" name="username" id="username" class="form-control" value="<%= user.username %>">
              <% if (errorMessages?.username) { %>
                <div class="text-danger small"><%= errorMessages.username %></div>
              <% } %>
            </div>
          </div>

          <div class="mb-4">
            <label for="email" class="form-label">Email</label>
            <input type="email" name="email" id="email" class="form-control" value="<%= user.email %>">
            <% if (errorMessages?.email) { %>
              <div class="text-danger small"><%= errorMessages.email %></div>
            <% } %>
          </div>

          <hr>
          <h4 class="mb-3">Change Password</h4>

          <div class="mb-3">
            <label for="currentPassword" class="form-label">Current Password</label>
            <div class="input-group">
              <input type="password" name="currentPassword" id="currentPassword" class="form-control password">
              <button type="button" class="btn btn-outline-secondary show-password" id="show-password" data-target="currentPassword">Show</button>
            </div>
            <% if (errorMessages?.currentPassword) { %>
              <div class="text-danger small"><%= errorMessages.currentPassword %></div>
            <% } %>
          </div>

          <div class="mb-3">
            <label for="newPassword" class="form-label">New Password</label>
            <div class="input-group">
              <input type="password" name="newPassword" id="newPassword" class="form-control password">
              <button type="button" class="btn btn-outline-secondary show-password" id="show-password" data-target="newPassword">Show</button>
            </div>
            <% if (passwordErrors?.length > 0) { %>
              <ul class="small mt-2">
                <% passwordErrors.forEach(error => { %>
                  <li class="text-danger"><%= error %></li>
                <% }) %>
              </ul>
            <% } %>
          </div>
        </div>
      </div>

      <div class="text-center mt-4">
        <button type="submit" class="btn btn-primary me-2">Save Changes</button>
        <a href="/users/profile" class="btn btn-secondary">Cancel</a>
      </div>
    </form>
  </div>
</div>

</body>
</html>